#event_simpleName=ProcessRollup2 OR #event_simpleName=Event_EppDetectionSummaryEvent AND (
    (FileName="mshta.exe"
        AND (
            CommandLine="*http://*" OR CommandLine="*https://*" OR CommandLine="*file://*" OR CommandLine="*vbscript:*" OR CommandLine="*javascript:*" OR CommandLine="*shell.application*" OR CommandLine="*adodb.stream*"
        )
        AND (CommandLine="*.hta*" OR CommandLine="*.sct*" OR CommandLine="*.js*" OR CommandLine="*.vbs*" OR CommandLine="*.ps1*" OR CommandLine="*.txt*" OR CommandLine="*.png*")
        AND ParentBaseFileName!="QualysAgent.exe" AND ParentBaseFileName!="ndtrack.exe" AND ParentBaseFileName!="gc_worker.exe"
    )
    OR (FileName="mshta.exe"
        AND (CommandLine="*\\AppData\\Local\\Temp\\*" OR CommandLine="*\\Windows\\Temp\\*")
        AND CommandLine="*.hta*"
        AND ParentBaseFileName!="QualysAgent.exe" AND ParentBaseFileName!="ndtrack.exe" AND ParentBaseFileName!="gc_worker.exe"
    )
)
| groupBy([CommandLine, FileName, ParentBaseFileName], function=[collect(ComputerName), count(aid, distinct=true, as=HostCount), count(as=EventCount)], limit=200)
| table([FileName, CommandLine, ParentBaseFileName, HostCount, EventCount, ComputerName])
